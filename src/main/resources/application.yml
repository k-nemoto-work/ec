spring:
  flyway:
    enabled: true
    locations: classpath:db/migration

  datasource:
    url: jdbc:postgresql://db:5432/ec_dev
    username: ec_user
    password: ec_pass
    driver-class-name: org.postgresql.Driver
  
server:
  port: 8080

logging:
  level:
    org.springframework: INFO
